export * from "./icon"

export const enum ViewId {
  Explorer = "facility.views.explorer",
  Outline = "facility.views.outline",
}

export const enum ViewName {
  Explorer = "explorer",
  Outline = "outline",
}

export enum ContextValues {
  Repositories = "facility:repositories",
  Outline = "facility:outline",
  Message = "facility:message",
  Explorer = "facility:explorer",
}

export const extensionId = "facility"
